<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="using:ChronoGit.Views"
             xmlns:vm="using:ChronoGit.ViewModels"
             xmlns:m="using:ChronoGit.Models"
             x:Class="ChronoGit.Views.EditView"
             x:DataType="vm:EditViewModel">

    <UserControl.Resources>
        <vm:SelectionToColorConverter x:Key="SelectionToColorConverter"/>
    </UserControl.Resources>

    <Grid RowDefinitions="10,Auto,Auto,10" ColumnDefinitions="60,10,100,Auto"
          Background="{Binding Selected, Converter={StaticResource SelectionToColorConverter}}">

        <v:CommitIconView Grid.Row="0" Grid.Column="0" Grid.RowSpan="4"/>
        <TextBlock Grid.Row="1" Grid.Column="2" Grid.RowSpan="2" VerticalAlignment="Center" Text="{Binding Id}" TextDecorations="Strikethrough" Foreground="Gray"/>
        <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding MessageShort}" FontSize="16"/>
        <TextBlock Grid.Row="2" Grid.Column="3" Text="{Binding FullAuthor}"/>
    </Grid>
</UserControl>
